[build]
  command = "npm install -g vite && npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18.17.0"
  NPM_FLAGS = "--no-optional"
  NODE_ENV = "production"

[functions]
  included_files = ["dist/**/*"] 